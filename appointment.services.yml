services:
  appointment.manager:
    class: Drupal\appointment\Service\AppointmentManager
    arguments:
      - '@entity_type.manager'

  appointment.email:
    class: Drupal\appointment\Service\EmailService
    arguments:
      - '@plugin.manager.mail'
      - '@language_manager'
      - '@entity_type.manager'
